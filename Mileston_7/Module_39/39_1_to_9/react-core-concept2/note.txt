///////////////39-2 Different ways to handle events in React//////////////
🧩 Definition

This App component demonstrates four different ways to handle button clicks in React using both regular and arrow functions.

💡 Example Breakdown

Normal Function Declaration

function handleClick() {
  alert('I am clicked');
}


👉 Bound when the component renders — called in the onClick directly.

<button onClick={handleClick}>Click with normal function</button>


Arrow Function Declaration

const handleClick2 = () => {
  alert('I am clicked 2');
}


👉 Works the same, just written in modern arrow syntax.

<button onClick={handleClick2}>Click2 with arrow function</button>


Inline Function (inside JSX)

<button onClick={() => alert('I am clicked 3')}>Click3 function in event handler</button>


👉 Function is defined directly inside the onClick.
⚠️ Creates a new function every render (not a problem here, but not efficient for large apps).

Event Handler with Argument

const handleAddnum = (num) => {
  alert(num + 5);
}


👉 You can pass arguments using an inline function:

<button onClick={() => handleAddnum(10)}>Click to add 5</button>


✅ Shows 15 in the alert.

⚙️ Use Case

This example teaches different click-handling patterns you’ll use throughout React apps — for buttons, form actions, modals, and more







/////////////////39-3 A simple Introduction to State change in React//////////////
🧩 Definition

This component uses React’s useState hook to store and update a counter when the user clicks a button.

💡 Example Breakdown
const [count, setCount] = useState(0)


count → holds the current number (initial value = 0).

setCount → function used to update that number.
When called, React re-renders the component with the new value.

⚙️ Event Handler
const handleAdd = () => {
  setCount(count + 5)
}


Each click increases the count by 5.

The UI automatically updates — no manual DOM changes needed (React does it for you).

💻 Rendered Output

When you click the Add button:

Count: 0  → 5  → 10  → 15  → ...

✅ Use Case

This same logic can power:

A shopping cart item count 🛒

A game score counter 🎮

A likes/upvotes feature 👍

A dynamic total in any calculation